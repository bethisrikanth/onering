# -----------------------------------------------------------------------------
# post: yum

#
# Hax Township - Pop. You
#
# manually add yum repos
#
/usr/bin/curl http://chefmaster.outbrain.com/boot/repos.tar.gz -o /tmp/repos.tar.gz
tar xf /tmp/repos.tar.gz -C /etc/yum.repos.d/

/usr/bin/yum clean all
/usr/bin/yum -y upgrade