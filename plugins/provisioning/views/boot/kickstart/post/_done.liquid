# -----------------------------------------------------------------------------
# post: done
#
echo 'System Installation Complete!'

# tell onering that the installation is complete
if [ -f /etc/hardware.id ]; then
  /usr/bin/curl -s http://onering.outbrain.com/api/provision/$(cat /etc/hardware.id)/boot/local
fi
